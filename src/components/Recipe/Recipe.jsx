import React from 'react';

const Recipe = ({recipe}) => {
    // console.log(recipe);

    const {name, ingredients, cooking_method,rating} = recipe;
    console.log(name);
    
    
    return (
        <div>
            
        </div>
    );
};

export default Recipe;